import math
from urllib import response

def estimate(var1, var2 ,var3):
    estimate = int(var1) + int(var2) + int(var3)
    response = f'Hej! Enligt måtten du angivit så kan spara upp till {estimate}, beroende på valet av fläkt modell. Har du några frågor?'
    return response
